# Prompt the user for their first number and assign it to a variable
prompt_0 = 'What is your first number?'
num_0 eval(input(prompt_0))

# Prompt the user for their second number and assign it to a variable
prompt_1 = 'What is your second number?'
num_1 eval(input(prompt_1))

# Add both number together and assign it to the variable: result
result = num_0 + num_1

# Print the result along with a string explaining it
print('The result of adding ' + str(num_0) + ' and ' + str(num_0) + ' is: ' str(result))
